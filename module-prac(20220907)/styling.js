export default function styling (element,width,height,color,display){
  element.style.width = width;
  element.style.height = height;
  element.style.backgroundColor = color;
  element.style.display = display;
}

// styling 함수 생성